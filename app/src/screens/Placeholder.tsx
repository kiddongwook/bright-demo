export function Placeholder({ name }: { name: string }) {
  return <section className="view on"><div className="head"><h1 className="hello">{name}</h1><p className="lede">곧 붙습니다.</p></div></section>;
}
