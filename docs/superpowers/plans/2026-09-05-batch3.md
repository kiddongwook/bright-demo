# 3차 묶음 — 남은 항목 순차 처리 (2026-09-05)

사용자 지시: "순차적으로 끝까지". 파일 겹침 기준으로 나눠 병렬 실행, 묶음마다 이음새·화면 검증 뒤 배포.

## 1차 (병렬 4)
| 에이전트 | 항목 | 파일 |
|---|---|---|
| R | 학생 연속 추가("저장하고 다음"), 연락처 붙여넣기 인식, 명부 행 길게 누르기(전화·초대·편집) | `Roster.tsx`, `ux.css` 추가분, `lib/contacts.ts` |
| C | 반·시간표 요일 시간 묶음 입력, 반 삭제 안내 문구(공지·휴원일 걸림) | `Calendar.tsx`, `classes.css`(신규) |
| N | 공지 대상 여러 반(`notice_targets`), 공지 상세 읽음률 도넛 + "안 읽은 N명에게 다시 알리기" | `0021_notice_targets.sql`, `Notices.tsx`, `NoticeRead.tsx`, `api.ts`(추가), `notices.css`(신규), `_shared`/트리거 |
| V | 학부모 홈 두 장 원칙 + 할 것 진행률, 리스트 행 규격 통일, 빈 상태 일러스트, 본문 칸 자동 늘어나기, 금액 콤마, 안드로이드 배지 아이콘 | `Child.tsx`, `Empty.tsx`, `icons.tsx`, `theme.css`, `ui.css`, `Billing.tsx`, `components/AutoTextarea.tsx`, `public/logo/badge.png`, `push-sw.js` |

## 2차 (1차 배포 뒤)
| 에이전트 | 항목 |
|---|---|
| L | 점검 낮음 항목: INT-06(하루 휴원일도 RPC), INT-11(같은 내용 알림 묶기), INT-26(명부 제외자 옛 알림 읽기), INT-35/36(전체·반 휴원 우선 규칙 명시), INP 낮음(날짜 표시 방어·CSV 진행 표시·금액 상한 안내) |
| M | 학원별 설치 이름·아이콘(동적 매니페스트·apple 메타), 소개 페이지 `?a=학원` 로 이름·로고·색 반영 |

## 검증
- 각 묶음 뒤: 단위·tsc·build, 회귀(seam·hardening·billing·manage·rls·outbox·push·invite·attendance-note), 전수 스크린샷(폰 밝음/어두움 + PC), 적대적 스크립트 중 관련 항목 재실행.
