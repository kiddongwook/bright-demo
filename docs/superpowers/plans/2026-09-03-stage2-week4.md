# 2단계 4주차 Implementation Plan — 운영 관리: 명부 편집·퇴원, 학생 상세(출결 달력·타임라인·메모), 휴원일·시간표, 보강 완결, 강사, FAQ 편집

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 원장이 앱 안에서 학원을 실제로 굴릴 수 있게 한다 — 학생을 넣고·반을 옮기고·번호를 바꾸고·퇴원시키고, 학생 하나를 열면 출결 달력·결석/문의/상담이 한 줄로 보이고, 휴원일을 정하면 출결·보강·"다음 수업"이 그걸 따르고, 보강을 실제로 왔는지 닫고, 강사를 명부에 넣고, FAQ 를 고친다. 스펙 §11 "2단계에 넣는다" 8개 중 남은 7개.

**Architecture:** 명부는 `roster_phones` 에 정책이 없어(서비스 키만) **RPC 두 개**(`roster_save_student`, `student_leave`)가 학생·반·번호를 한 트랜잭션으로 맞춘다 — 화면은 RPC 만 부른다. 나머지(메모·휴원일·시간표·보강 완결)는 기존 RLS 정책(staff 쓰기)으로 테이블을 직접 만진다. 학생 상세는 화면 하나에 세 단(출결 달력 · 타임라인 · 메모). 휴원일은 `calendar(kind='closed')` 한 곳에서 읽어 `nextClassDays` 가 건너뛰고 오늘 화면이 표시한다. 강사는 명부에 `role='teacher'` 행을 넣으면 OTP 로 들어오며 staff 로 취급된다(반 담당 지정은 `classes.teacher_id`, **자기 반으로 RLS 를 좁히는 건 5주차** — 이번 주엔 원장과 같은 권한임을 화면에 적는다).

**Tech Stack:** 1~3주차와 동일. 통합 테스트 `tools/manage-test.mjs`(원장 세션으로 RPC·테이블 쓰기 → 학부모 세션으로 차단 확인). 단위 테스트는 `nextClassDays` 휴원일 건너뛰기·월 달력 계산.

## Global Constraints

- 스펙 §3·§4·§11 · 1~3주차 계획의 전역 제약 그대로.
- 마이그레이션은 추가만(`0007_manage.sql`). 적용된 파일은 수정하지 않는다.
- 화면은 DB 를 직접 만지지 않는다 — `app/src/lib/api.ts` 만. 명부·퇴원은 RPC 만.
- 퇴원은 **데이터 보존**: `students.status='left'`, `left_at`; 출결·문의·메모는 남는다. 접근 차단은 그 학생에 딸린 `memberships`·`guardians`·`roster_phones` 삭제로(학생 본인 계정의 membership 포함). `users` 행은 남긴다(다른 학원·다른 자녀가 있을 수 있다).
- 전화번호는 `normalize_phone()` 으로 숫자만 저장. 같은 번호가 학부모로 두 자녀에 걸리는 건 정상(자녀마다 roster 행 하나).
- 날짜는 KST. 달력은 월요일 시작.
- 문구는 데모 톤 그대로(원장님·학부모·~해요).

---

## File Structure

| 경로 | 책임 |
|---|---|
| `supabase/migrations/0007_manage.sql` | RPC `roster_save_student`·`student_leave`·`student_timeline`·`month_attendance`; `absence_requests` 보강 완결 RPC `makeup_attended`; `students_read` 에 퇴원생 포함(원장) 확인 |
| `tools/manage-test.mjs` | 명부 저장→로그인 가능, 반 이동, 번호 교체, 퇴원→차단, 메모, 휴원일, 보강 완결, 타임라인, 월 출결 |
| `app/src/lib/api.ts` | `saveStudent`·`leaveStudent`·`studentDetail`·`monthAttendance`·`timeline`·`listNotes/addNote/deleteNote`·`listCalendar/addCalendar/removeCalendar`·`updateClassSchedule/createClass`·`markMakeupAttended`·`saveFaq/deleteFaq`·`listTeachers/saveTeacher`; `nextClassDays(schedule, count, closed?)`; `monthGrid(ym)` |
| `app/src/lib/dates.test.ts` | `nextClassDays` 휴원일 건너뛰기, `monthGrid` |
| `app/src/screens/director/Roster.tsx` | 명부(반별·퇴원생 접기) · 학생 편집(`student-edit`) · 강사(`teachers`) — `More.tsx` 의 `Roster` 를 옮긴다 |
| `app/src/screens/director/StudentDetail.tsx` | 학생 상세(`student`): 출결 달력 · 타임라인 · 메모 |
| `app/src/screens/director/Calendar.tsx` | 휴원일·특강(`calendar`) · 반 시간표(`classes`) |
| `app/src/screens/director/Today.tsx` | 휴원일 표시, 보강 완결 단추 |
| `app/src/screens/director/Inbox.tsx` | `FaqManage` 에 추가·수정·삭제 |
| `app/src/screens/director/More.tsx` | 운영 목록에 학생 상세 진입·휴원일·시간표·강사 행 |
| `app/src/screens/parent/Child.tsx` · `student/Me.tsx` | `nextClassDays` 에 휴원일 전달, 결석 신청 날짜 후보에서 휴원일 제외 |
| `app/src/lib/nav.tsx` · `screens/registry.tsx` | 새 뷰 `student`·`student-edit`·`teachers`·`calendar`·`classes` |
| `supabase/functions/link-login/index.ts` | 자녀 둘인 학부모: 링크의 ref 로 자녀 소속 고르기 (3주차 남긴 것) |

---

### Task 1: `0007_manage.sql` — 명부 RPC · 퇴원 · 타임라인 · 월 출결 · 보강 완결

**Files:**
- Create: `supabase/migrations/0007_manage.sql`
- Create: `tools/manage-test.mjs`
- Modify: `tools/cleanup-test-data.mjs:5` — slug 패턴에 `manage-%`

**Interfaces (Produces):**
- `roster_save_student(sid uuid, p_name text, p_class_ids uuid[], p_student_phone text, p_parent_phones text[]) returns uuid` — staff 전용. `sid` null 이면 새 학생. 이름·`enrollments`·`roster_phones`(학생 1 + 학부모 n)를 **그 학생 기준으로 전부 맞춘다**(없어진 번호의 roster 행은 지우고, 그 번호로 이미 들어와 있던 membership 도 지운다). 이미 들어와 있는 사용자(`users.phone`)가 있으면 `memberships`·`guardians`·`students.user_id` 를 바로 이어 준다(다음 로그인을 기다리지 않게).
- `student_leave(sid uuid) returns void` — staff 전용. `status='left', left_at=now()`, 그 학생의 `memberships`·`guardians`·`roster_phones`·`enrollments` 삭제.
- `student_timeline(sid uuid, lim int) returns table (at timestamptz, kind text, title text, body text, ref uuid)` — staff 전용. 출결(지각·결석·보강만)·결석 신청·문의·메모를 시간순.
- `month_attendance(sid uuid, ym text) returns table (date date, status att_status)` — 자기 자녀·본인·staff. `ym = 'YYYY-MM'`.
- `makeup_attended(aid uuid) returns void` — staff 전용. `absence_requests.attended_at=now()` + `attendance(status='makeup', date=보강일, class_id=그 학생 첫 반)` upsert. `makeup_kind='material'` 이면 attendance 없이 `attended_at` 만.
- `students_read` 정책은 이미 staff 에게 퇴원생을 보여준다(status 조건 없음) — 화면에서 나눈다.

- [ ] **Step 1: 실패하는 테스트**

`tools/manage-test.mjs`:
```js
// 4주차 통합 테스트: 명부 저장 → 학부모 로그인 가능 → 반 이동 → 번호 교체 → 퇴원 → 차단, 메모·휴원일·보강 완결·타임라인·월 출결
import 'dotenv/config';
import { createClient } from '@supabase/supabase-js';
const URL = process.env.SUPABASE_URL, SVC = process.env.SUPABASE_SERVICE_KEY, ANON = process.env.SUPABASE_ANON_KEY;
const admin = createClient(URL, SVC, { auth: { persistSession: false } });
const fails = []; const ok = (c, m) => { if (!c) fails.push(m); };
const rnd = Math.random().toString(36).slice(2, 8); const num = () => String(Math.floor(Math.random() * 1e6)).padStart(6, '0');
const kst = (off = 0) => new Date(Date.now() + 9 * 3600e3 + off * 86400e3).toISOString().slice(0, 10);
const PW = 'manage-' + rnd; const email = p => `${p}@auth.yeongeo.local`;

const { data: ac } = await admin.from('academies').insert({ slug: `manage-${rnd}`, name: '관리 테스트' }).select().single(); const A = ac.id;
const { data: c1 } = await admin.from('classes').insert({ academy_id: A, name: '고1 A', schedule: [{ dow: 1, start: '19:00', end: '21:00' }, { dow: 3, start: '19:00', end: '21:00' }] }).select().single();
const { data: c2 } = await admin.from('classes').insert({ academy_id: A, name: '고2 B', schedule: [{ dow: 2, start: '20:00', end: '22:00' }] }).select().single();
// 원장: auth 사용자 + membership (다른 테스트와 같은 방식)
const P_DIR = '0109' + num() + '3';
const { data: du } = await admin.auth.admin.createUser({ email: email(P_DIR), password: PW, email_confirm: true });
await admin.from('users').insert({ id: du.user.id, name: '김지영', phone: P_DIR });
const { data: dm } = await admin.from('memberships').insert({ user_id: du.user.id, academy_id: A, role: 'director' }).select().single();
await admin.from('users').update({ active_membership_id: dm.id }).eq('id', du.user.id);
const d = createClient(URL, ANON, { auth: { persistSession: false } }); ok(!(await d.auth.signInWithPassword({ email: email(P_DIR), password: PW })).error, '원장 로그인');

// A. 새 학생 저장 → roster 행 3개(학생1·학부모2), enrollments 1
const P_ST = '0109' + num() + '1', P_MOM = '0109' + num() + '2', P_DAD = '0109' + num() + '4';
let r = await d.rpc('roster_save_student', { sid: null, p_name: '박지훈', p_class_ids: [c1.id], p_student_phone: '010-' + P_ST.slice(3, 7) + '-' + P_ST.slice(7), p_parent_phones: [P_MOM, P_DAD] });
ok(!r.error, 'roster_save_student(new): ' + r.error?.message); const SID = r.data;
let { data: rp } = await admin.from('roster_phones').select('phone, role').eq('student_id', SID);
ok(rp?.length === 3 && rp.filter(x => x.role === 'parent').length === 2 && rp.some(x => x.phone === P_ST && x.role === 'student'), `roster 3행 (got ${JSON.stringify(rp)})`);
let { data: en } = await admin.from('enrollments').select('class_id').eq('student_id', SID); ok(en?.length === 1 && en[0].class_id === c1.id, '반 1개');

// B. 학부모(엄마)가 OTP 로 들어올 수 있다 = roster 대조 통과 (otp-send 는 roster_phones 만 본다)
const send = await fetch(`${URL}/functions/v1/otp-send`, { method: 'POST', headers: { 'Content-Type': 'application/json', apikey: ANON }, body: JSON.stringify({ phone: P_MOM }) });
ok(send.status === 200, `엄마 otp-send 200 (got ${send.status})`);
// 엄마를 실제 사용자로 (verify 대신 admin 으로 만들고 로그인) → roster_save_student 가 기존 사용자를 이어 주는지 아래 D 에서 본다
const { data: mu } = await admin.auth.admin.createUser({ email: email(P_MOM), password: PW, email_confirm: true });
await admin.from('users').insert({ id: mu.user.id, name: '박지훈 어머님', phone: P_MOM });
const { data: mm } = await admin.from('memberships').insert({ user_id: mu.user.id, academy_id: A, role: 'parent', student_id: SID }).select().single();
await admin.from('guardians').insert({ student_id: SID, user_id: mu.user.id });
await admin.from('users').update({ active_membership_id: mm.id }).eq('id', mu.user.id);
const p = createClient(URL, ANON, { auth: { persistSession: false } }); ok(!(await p.auth.signInWithPassword({ email: email(P_MOM), password: PW })).error, '엄마 로그인');
ok(((await p.from('students').select('id').eq('id', SID)).data ?? []).length === 1, '엄마가 자녀를 본다');

// C. 반 이동(고1 A → 고2 B) + 아빠 번호 제거 + 이름 수정
r = await d.rpc('roster_save_student', { sid: SID, p_name: '박지훈(수정)', p_class_ids: [c2.id], p_student_phone: P_ST, p_parent_phones: [P_MOM] });
ok(!r.error, 'roster_save_student(edit): ' + r.error?.message);
({ data: en } = await admin.from('enrollments').select('class_id').eq('student_id', SID)); ok(en?.length === 1 && en[0].class_id === c2.id, '반 이동');
({ data: rp } = await admin.from('roster_phones').select('phone').eq('student_id', SID)); ok(rp?.length === 2 && !rp.some(x => x.phone === P_DAD), '아빠 번호 제거');
ok((await admin.from('students').select('name').eq('id', SID).single()).data?.name === '박지훈(수정)', '이름 수정');

// D. 새 학부모 번호가 이미 사용자면 바로 이어 준다: 아빠를 사용자로 만든 뒤 번호를 다시 넣는다
const { data: fu } = await admin.auth.admin.createUser({ email: email(P_DAD), password: PW, email_confirm: true });
await admin.from('users').insert({ id: fu.user.id, name: '박지훈 아버님', phone: P_DAD });
r = await d.rpc('roster_save_student', { sid: SID, p_name: '박지훈', p_class_ids: [c2.id], p_student_phone: P_ST, p_parent_phones: [P_MOM, P_DAD] });
ok(!r.error, 'roster_save_student(re-add dad): ' + r.error?.message);
ok(((await admin.from('memberships').select('id').eq('user_id', fu.user.id).eq('student_id', SID)).data ?? []).length === 1, '아빠 membership 자동 연결');
ok(((await admin.from('guardians').select('user_id').eq('student_id', SID).eq('user_id', fu.user.id)).data ?? []).length === 1, '아빠 guardians 자동 연결');

// E. 메모·휴원일·출결·보강 완결
r = await d.from('notes').insert({ academy_id: A, student_id: SID, author_id: du.user.id, kind: 'consult', body: '어머님 상담: 단어 암기 계획' }).select().single(); ok(!r.error, '메모 insert: ' + r.error?.message);
ok(!(await p.from('notes').select('id').eq('student_id', SID)).data?.length, '학부모는 메모를 못 본다');
r = await d.from('calendar').insert({ academy_id: A, date: kst(1), kind: 'closed', note: '휴원' }).select().single(); ok(!r.error, '휴원일 insert: ' + r.error?.message);
ok(((await p.from('calendar').select('date').eq('academy_id', A)).data ?? []).length === 1, '학부모도 휴원일은 본다');
await admin.from('attendance').upsert([{ academy_id: A, student_id: SID, class_id: c2.id, date: kst(-7), status: 'present', marked_by: du.user.id }, { academy_id: A, student_id: SID, class_id: c2.id, date: kst(-2), status: 'late', marked_by: du.user.id }], { onConflict: 'student_id,class_id,date' });
const { data: ab } = await admin.from('absence_requests').insert({ academy_id: A, student_id: SID, requested_by: mu.user.id, date: kst(-1), reason: '병원', status: 'confirmed', makeup_kind: 'saturday', makeup_at: kst(2) + 'T14:00:00+09:00', decided_by: du.user.id }).select().single();
r = await d.rpc('makeup_attended', { aid: ab.id }); ok(!r.error, 'makeup_attended: ' + r.error?.message);
ok(!!(await admin.from('absence_requests').select('attended_at').eq('id', ab.id).single()).data?.attended_at, 'attended_at 기록');
ok((await admin.from('attendance').select('status').eq('student_id', SID).eq('date', kst(2)).maybeSingle()).data?.status === 'makeup', '보강 출결 makeup 행');

// F. 월 출결 (학부모도) · 타임라인 (원장만)
const ym = kst(0).slice(0, 7);
r = await p.rpc('month_attendance', { sid: SID, ym }); ok(!r.error && Array.isArray(r.data) && r.data.some(x => x.status === 'late'), `month_attendance(학부모): ${r.error?.message ?? r.data?.length}`);
r = await d.rpc('student_timeline', { sid: SID, lim: 50 });
ok(!r.error && r.data.length >= 4 && r.data.some(x => x.kind === 'note') && r.data.some(x => x.kind === 'absence') && r.data.some(x => x.kind === 'attendance'), `timeline: ${r.error?.message ?? JSON.stringify(r.data?.map(x => x.kind))}`);
ok((await p.rpc('student_timeline', { sid: SID, lim: 10 })).error, '학부모는 타임라인 RPC 거절');

// G. 퇴원 → 학부모 차단, 데이터 보존
r = await d.rpc('student_leave', { sid: SID }); ok(!r.error, 'student_leave: ' + r.error?.message);
const st = (await admin.from('students').select('status,left_at').eq('id', SID).single()).data; ok(st?.status === 'left' && st.left_at, '학생 status left');
ok(((await admin.from('roster_phones').select('id').eq('student_id', SID)).data ?? []).length === 0, 'roster 행 삭제');
ok(((await admin.from('memberships').select('id').eq('student_id', SID)).data ?? []).length === 0, 'membership 삭제');
ok(((await admin.from('attendance').select('id').eq('student_id', SID)).data ?? []).length >= 2, '출결은 남는다');
const gone = await fetch(`${URL}/functions/v1/otp-send`, { method: 'POST', headers: { 'Content-Type': 'application/json', apikey: ANON }, body: JSON.stringify({ phone: P_MOM }) });
ok(gone.status === 404, `퇴원 뒤 엄마 otp-send 404 (got ${gone.status})`);
const p2 = createClient(URL, ANON, { auth: { persistSession: false } }); await p2.auth.signInWithPassword({ email: email(P_MOM), password: PW });
ok(!((await p2.from('students').select('id').eq('id', SID)).data ?? []).length, '남은 세션으로도 자녀를 못 본다');

if (fails.length) { console.error('FAIL\n- ' + fails.join('\n- ')); process.exitCode = 1; } else console.log('PASS: manage A~G');
```
`tools/cleanup-test-data.mjs` 의 slug 패턴에 `slug.like.manage-%` 추가.

- [ ] **Step 2: 실패 확인** — `cd tools && node --env-file=../.env.local manage-test.mjs` → `roster_save_student` 없음.

- [ ] **Step 3: 마이그레이션**

`supabase/migrations/0007_manage.sql`:
```sql
-- 4주차: 명부 편집·퇴원·타임라인·월 출결·보강 완결. roster_phones 는 정책이 없으므로 RPC(security definer)로만 만진다.

-- 학생 하나의 이름·반·번호를 통째로 맞춘다. sid null 이면 새 학생.
create or replace function roster_save_student(sid uuid, p_name text, p_class_ids uuid[], p_student_phone text, p_parent_phones text[])
returns uuid language plpgsql security definer set search_path = public as $$
declare a uuid := current_academy_id(); s uuid := sid; ph text; u uuid; m uuid; keep text[] := '{}';
begin
  if not is_staff() then raise exception 'not allowed'; end if;
  if s is null then insert into students (academy_id, name) values (a, trim(p_name)) returning id into s;
  else
    if not exists (select 1 from students where id = s and academy_id = a) then raise exception 'not found'; end if;
    update students set name = trim(p_name) where id = s;
  end if;
  -- 반: 주어진 목록으로 맞춘다 (모두 이 학원 반이어야)
  if exists (select 1 from unnest(p_class_ids) c where c not in (select id from classes where academy_id = a)) then raise exception 'bad class'; end if;
  delete from enrollments where student_id = s and class_id <> all(coalesce(p_class_ids, '{}'));
  insert into enrollments (student_id, class_id) select s, c from unnest(coalesce(p_class_ids, '{}')) c on conflict do nothing;
  -- 번호: 학생 1 + 학부모 n. 이미 들어와 있는 사용자면 바로 이어 준다.
  ph := normalize_phone(p_student_phone);
  if ph <> '' then
    keep := keep || ph;
    insert into roster_phones (academy_id, phone, role, name, student_id) values (a, ph, 'student', trim(p_name), s) on conflict (academy_id, phone, role, student_id) do update set name = excluded.name;
    select id into u from users where phone = ph;
    if u is not null then
      insert into memberships (user_id, academy_id, role, student_id) values (u, a, 'student', s) on conflict (user_id, academy_id, role, student_id) do nothing;
      update students set user_id = u where id = s and user_id is null;
    end if;
  end if;
  foreach ph in array coalesce(p_parent_phones, '{}') loop
    ph := normalize_phone(ph); if ph = '' then continue; end if;
    keep := keep || ph;
    insert into roster_phones (academy_id, phone, role, name, student_id) values (a, ph, 'parent', trim(p_name) || ' 학부모', s) on conflict (academy_id, phone, role, student_id) do nothing;
    select id into u from users where phone = ph;
    if u is not null then
      insert into memberships (user_id, academy_id, role, student_id) values (u, a, 'parent', s) on conflict (user_id, academy_id, role, student_id) do nothing;
      insert into guardians (student_id, user_id) values (s, u) on conflict do nothing;
    end if;
  end loop;
  -- 빠진 번호: roster 행과 그 번호로 이어진 membership·guardian 을 지운다
  for ph, u in select rp.phone, us.id from roster_phones rp left join users us on us.phone = rp.phone where rp.student_id = s and rp.phone <> all(keep) loop
    if u is not null then delete from memberships where user_id = u and student_id = s; delete from guardians where user_id = u and student_id = s; end if;
  end loop;
  delete from roster_phones where student_id = s and phone <> all(keep);
  return s;
end $$;

-- 퇴원: 데이터는 남기고 접근만 끊는다
create or replace function student_leave(sid uuid) returns void language plpgsql security definer set search_path = public as $$
begin
  if not is_staff() or not exists (select 1 from students where id = sid and academy_id = current_academy_id()) then raise exception 'not allowed'; end if;
  update students set status = 'left', left_at = now() where id = sid;
  delete from memberships where student_id = sid;
  delete from guardians where student_id = sid;
  delete from roster_phones where student_id = sid;
  delete from enrollments where student_id = sid;
end $$;

-- 학생 타임라인 (원장·강사)
create or replace function student_timeline(sid uuid, lim int) returns table (at timestamptz, kind text, title text, body text, ref uuid)
language sql stable security definer set search_path = public as $$
  select * from (
    select a.created_at, 'attendance', to_char(a.date, 'MM/DD') || ' ' || case a.status when 'late' then '지각' when 'absent' then '결석' else '보강 출석' end, coalesce(a.note, ''), a.id
      from attendance a where a.student_id = sid and a.academy_id = current_academy_id() and a.status in ('late','absent','makeup')
    union all
    select r.created_at, 'absence', to_char(r.date, 'MM/DD') || ' 결석 신청' || case r.status when 'confirmed' then ' → ' || case when r.makeup_kind = 'material' then '자료 대체' else '보강 ' || to_char(r.makeup_at at time zone 'Asia/Seoul', 'MM/DD HH24:MI') end || case when r.attended_at is not null then ' (완료)' else '' end when 'declined' then ' (보류)' else ' (확인 중)' end, r.reason, r.id
      from absence_requests r where r.student_id = sid and r.academy_id = current_academy_id()
    union all
    select i.created_at, 'inquiry', '문의: ' || i.topic || case when i.answer is not null then ' (답변함)' else ' (답변 대기)' end, i.body, i.id
      from inquiries i where i.student_id = sid and i.academy_id = current_academy_id()
    union all
    select n.created_at, 'note', case n.kind when 'consult' then '상담' else '메모' end, n.body, n.id
      from notes n where n.student_id = sid and n.academy_id = current_academy_id()
  ) t(at, kind, title, body, ref)
  where is_staff()
  order by at desc limit lim $$;

-- 월 출결 (자기 자녀·본인·staff)
create or replace function month_attendance(sid uuid, ym text) returns table (date date, status att_status)
language sql stable security definer set search_path = public as $$
  select a.date, a.status from attendance a
  where a.student_id = sid and a.academy_id = current_academy_id()
    and (is_staff() or sid in (select my_student_ids()))
    and to_char(a.date, 'YYYY-MM') = ym
  order by a.date $$;

-- 보강 완결: 왔다고 표시 + 출결에 makeup 행
create or replace function makeup_attended(aid uuid) returns void language plpgsql security definer set search_path = public as $$
declare r absence_requests; cid uuid;
begin
  select * into r from absence_requests where id = aid and academy_id = current_academy_id();
  if r.id is null or not is_staff() or r.status <> 'confirmed' then raise exception 'not allowed'; end if;
  update absence_requests set attended_at = now() where id = aid;
  if r.makeup_kind = 'saturday' and r.makeup_at is not null then
    select e.class_id into cid from enrollments e join classes c on c.id = e.class_id where e.student_id = r.student_id order by c.name limit 1;
    if cid is not null then
      insert into attendance (academy_id, student_id, class_id, date, status, marked_by)
      values (r.academy_id, r.student_id, cid, (r.makeup_at at time zone 'Asia/Seoul')::date, 'makeup', auth.uid())
      on conflict (student_id, class_id, date) do update set status = 'makeup';
    end if;
  end if;
end $$;
```

- [ ] **Step 4: 적용·통과 확인** — `npx supabase db push` → `manage-test` PASS → `flow-test`·`rls-test` PASS → cleanup.

- [ ] **Step 5: 커밋** — `feat(db): 명부 RPC(저장·퇴원), 학생 타임라인·월 출결·보강 완결`

---

### Task 2: api 계층 + 날짜 유틸 + 단위 테스트

**Files:**
- Modify: `app/src/lib/api.ts`
- Create: `app/src/lib/dates.test.ts`

**Interfaces (Produces):**
```ts
export type StudentFull = Student & { status: 'active' | 'left'; left_at: string | null; student_phone: string; parent_phones: string[] };
export type Note = { id: string; kind: 'consult' | 'memo'; body: string; created_at: string; author_name: string };
export type CalItem = { id: string; date: string; kind: 'closed' | 'makeup' | 'special'; note: string | null; class_id: string | null };
export type TimelineItem = { at: string; kind: 'attendance' | 'absence' | 'inquiry' | 'note'; title: string; body: string; ref: string };
export type Teacher = { user_id: string | null; name: string; phone: string; membership_id: string | null };
listStudents(classId?, includeLeft = false)            // status 필터
studentDetail(sid): Promise<StudentFull>              // students + enrollments + roster_phones 는 RPC 로: roster_of_student(sid) (아래)
saveStudent(sid | null, name, classIds, studentPhone, parentPhones): Promise<string>   // rpc roster_save_student
leaveStudent(sid)                                     // rpc student_leave
monthAttendance(sid, ym): Promise<{date, status}[]>   // rpc
timeline(sid, lim = 50): Promise<TimelineItem[]>      // rpc
listNotes(sid) / addNote(sid, kind, body) / deleteNote(id)
listCalendar(fromYm) / addCalendar(date, kind, note, classId | null) / removeCalendar(id)
closedDays(): Promise<Set<string>>                    // calendar kind closed, 오늘부터 60일
createClass(name, schedule) / updateClass(id, name, schedule, teacherId | null)
markMakeupAttended(aid)                                // rpc
saveFaq(id | null, q, a) / deleteFaq(id)
listTeachers() / saveTeacher(name, phone)              // rpc roster_save_teacher (아래) / removeTeacher(phone)
nextClassDays(schedule, count, closed?: Set<string>)   // closed 제외
monthGrid(ym): { days: (string | null)[]; label: string; prev: string; next: string }  // 월요일 시작 6주 격자
```
- `roster_of_student(sid)` 와 `roster_save_teacher(p_name, p_phone)` · `roster_remove_teacher(p_phone)` 은 `0007` 에 함께 넣는다(RPC — roster_phones 는 정책이 없다):
```sql
create or replace function roster_of_student(sid uuid) returns table (phone text, role user_role) language sql stable security definer set search_path = public as $$
  select phone, role from roster_phones where student_id = sid and academy_id = current_academy_id() and is_staff() $$;
create or replace function roster_save_teacher(p_name text, p_phone text) returns void language plpgsql security definer set search_path = public as $$
declare ph text := normalize_phone(p_phone); u uuid;
begin
  if current_role_() <> 'director' then raise exception 'not allowed'; end if;
  insert into roster_phones (academy_id, phone, role, name) values (current_academy_id(), ph, 'teacher', trim(p_name)) on conflict (academy_id, phone, role, student_id) do update set name = excluded.name;
  select id into u from users where phone = ph;
  if u is not null then insert into memberships (user_id, academy_id, role) values (u, current_academy_id(), 'teacher') on conflict (user_id, academy_id, role, student_id) do nothing; end if;
end $$;
create or replace function roster_remove_teacher(p_phone text) returns void language plpgsql security definer set search_path = public as $$
declare ph text := normalize_phone(p_phone); u uuid;
begin
  if current_role_() <> 'director' then raise exception 'not allowed'; end if;
  delete from roster_phones where academy_id = current_academy_id() and phone = ph and role = 'teacher';
  select id into u from users where phone = ph;
  if u is not null then delete from memberships where user_id = u and academy_id = current_academy_id() and role = 'teacher'; update classes set teacher_id = null where academy_id = current_academy_id() and teacher_id = u; end if;
end $$;
create or replace function list_teachers() returns table (user_id uuid, name text, phone text) language sql stable security definer set search_path = public as $$
  select us.id, rp.name, rp.phone from roster_phones rp left join users us on us.phone = rp.phone
  where rp.academy_id = current_academy_id() and rp.role = 'teacher' and is_staff() order by rp.name $$;
```
(`memberships` 의 unique 제약 이름·컬럼은 0001 의 `unique (user_id, academy_id, role, student_id)` 를 쓴다. `student_id` 가 null 인 강사 행은 Postgres 의 null-distinct 규칙상 중복이 생길 수 있어 `do nothing` 앞에 `where not exists` 로 한 번 더 막는다 — 실행 시 확인.)

- [ ] **Step 1: 단위 테스트** `app/src/lib/dates.test.ts`
```ts
import { describe, it, expect } from 'vitest';
import { nextClassDays, monthGrid, kstDate, dowOf } from './api';
describe('nextClassDays + 휴원일', () => {
  it('휴원일은 건너뛴다', () => {
    const sched = [1, 2, 3, 4, 5, 6, 0].map(dow => ({ dow, start: '00:01', end: '23:59' })); // 매일 수업, 오늘은 시작 지나서 제외
    const [d1, d2] = nextClassDays(sched, 2);
    const withClosed = nextClassDays(sched, 2, new Set([d1]));
    expect(withClosed[0]).toBe(d2);
    expect(withClosed).not.toContain(d1);
  });
});
describe('monthGrid', () => {
  it('월요일 시작, 6주 42칸, 라벨·이웃 달', () => {
    const g = monthGrid('2026-09');
    expect(g.days.length).toBe(42);
    expect(g.days[1]).toBe('2026-09-01'); // 2026-09-01 은 화요일 → 첫 칸(월)은 null
    expect(g.days[0]).toBeNull();
    expect(g.label).toBe('2026년 9월'); expect(g.prev).toBe('2026-08'); expect(g.next).toBe('2026-10');
    expect(dowOf(g.days[1]!)).toBe(2);
  });
});
```
- [ ] **Step 2: 구현** — `nextClassDays` 에 세 번째 인자 `closed?: Set<string>`(있으면 `closed.has(iso)` 건너뜀), `monthGrid`, 위 함수들. `listStudents` 는 `status` 를 select 하고 `includeLeft` 아니면 `active` 만.
- [ ] **Step 3: `npm test` 통과, `tsc` 통과, 커밋** — `feat(api): 관리 api(명부·메모·휴원일·시간표·보강 완결·FAQ·강사), 휴원일 건너뛰는 다음 수업, 월 격자`

---

### Task 3: 명부 화면 — 목록·학생 편집·퇴원·강사

**Files:**
- Create: `app/src/screens/director/Roster.tsx` (`Roster`·`StudentEdit`·`Teachers`) — `More.tsx` 의 `Roster` 는 지운다
- Modify: `app/src/screens/registry.tsx`, `app/src/lib/nav.tsx` (`TITLE`: `'student-edit': ['학생', '편집']`, `teachers: ['강사', '']`, `student: ['학생', '']`; `PARENT_TAB` 에 셋 다 `more`)

**화면 계약:**
- `Roster`: 반별 묶음(활성 학생만) + 맨 아래 접힌 "퇴원 N명"(`details`). 학생 행을 누르면 `student`(상세, Task 4). 행 오른쪽 "편집" 작은 단추 → `student-edit?id`. 맨 아래 `학생 추가` → `student-edit`(id 없음). 상단 lede 에 "강사 N명 · 관리" 링크 → `teachers`.
- `StudentEdit`: 입력 `이름`, 반 체크(`.seg col` 다중 선택 — 눌러 토글), `학생 번호`(선택), `학부모 번호` 1~3개(빈 칸은 무시, `+ 번호 추가`). 저장 → `saveStudent` → toast "저장했어요. 번호가 있는 사람은 바로 들어올 수 있어요" → back. 편집일 때 맨 아래 위험 구역 "퇴원 처리" → `confirm('퇴원 처리하면 학부모·학생이 앱에 들어올 수 없어요. 기록은 남아요.')` → `leaveStudent` → toast → `nav.tab('more')`.
- `Teachers`: 목록(이름·번호·들어옴 여부) + 추가 폼(이름·번호) + 행마다 "빼기". 안내문: "강사는 원장님과 같은 권한으로 출결·공지·문의를 봅니다. 반별로 좁히는 건 다음 단계."
- 전화번호 입력은 `formatPhone`(1주차 `lib/phone.ts`) 로 보여주고 저장은 숫자만.

- [ ] Step 1: 화면 구현 · registry · TITLE
- [ ] Step 2: 빌드 → 브라우저: 원장으로 학생 추가(이름 "테스트 학생", 고1 A, 학부모 번호 010-1234-0999) → 명부에 보임 → 편집으로 반을 고2 B 로 → 퇴원 → "퇴원 1명" 아래로. `otp-send` 로 0999 번호가 404 인지 확인. 강사 추가(010-1234-0777) → 목록에 보임 → 빼기.
- [ ] Step 3: 커밋 — `feat(app): 명부 편집·학생 추가·퇴원·강사 관리`

---

### Task 4: 학생 상세 — 출결 달력 · 타임라인 · 메모

**Files:**
- Create: `app/src/screens/director/StudentDetail.tsx`
- Modify: `app/src/theme.css` — `.cal`(7열 격자), `.cal i`(날짜 칸: `p/l/a/m/closed/today`), `.tl`(타임라인 줄)
- Modify: `registry.tsx` (`director:student`), `parent/Child.tsx`(주간 줄 아래 "이번 달 달력" 링크는 5주차 — 이번 주는 원장만)

**화면 계약:**
- 머리: 이름 · 반 · 번호(학생/학부모, `formatPhone`) · 상태(퇴원이면 태그).
- 단 1 "출결": `monthGrid(ym)` 격자, `monthAttendance` 로 칠함(○ 출석·△ 지각·✕ 결석·◌ 보강), 휴원일은 회색, 오늘 테두리. 위에 `‹ 2026년 9월 ›`. 아래 요약 "출석 12 · 지각 1 · 결석 0 · 보강 1 · 출석률 92%"(출석률 = (출석+지각+보강)/(기록된 날)).
- 단 2 "기록": `timeline` 목록 — 왼쪽 종류 라벨(출결/결석/문의/상담·메모), 제목, 본문 한 줄, 날짜.
- 단 3 "메모": 입력(종류 `상담|메모` seg + textarea) → `addNote` → 목록 갱신; 행마다 삭제.
- 상단 오른쪽 "편집" → `student-edit?id`.

- [ ] Step 1: 구현 (단 전환은 `.seg` 세 칸, 상태 `tab: 'att' | 'log' | 'note'`)
- [ ] Step 2: 빌드 → 브라우저: 씨앗 김민수(고2 B) 상세 → 9월 달력에 오늘 지각(△) → 메모 "상담: 단어 암기" 추가 → 기록 단에 보임 → 스크린샷.
- [ ] Step 3: 커밋 — `feat(app): 학생 상세 — 월 출결 달력·타임라인·상담 메모`

---

### Task 5: 휴원일·특강 · 반 시간표 · 오늘 화면 연동 · 보강 완결

**Files:**
- Create: `app/src/screens/director/Calendar.tsx` (`CalendarScreen`: 휴원일·특강 목록/추가/삭제 · `Classes`: 반 목록·시간표 편집·담당 강사)
- Modify: `app/src/screens/director/Today.tsx` — 오늘이 휴원일이면 출석부 대신 안내("오늘은 휴원일이에요 · {note}"), 날짜 선택은 그대로; "처리됨" 목록의 보강 확정 행에 `보강 왔어요` 단추(`markMakeupAttended`) → 태그 "완료"
- Modify: `app/src/screens/parent/Child.tsx`, `app/src/screens/student/Me.tsx` — `closedDays()` 를 읽어 `nextClassDays(…, closed)`; `Absence` 날짜 후보도 같은 closed 전달
- Modify: `More.tsx` — 운영 목록에 `휴원일·특강`, `반·시간표` 행
- Modify: `nav.tsx` TITLE(`calendar: ['휴원일·특강', '']`, `classes: ['반·시간표', '']`), `PARENT_TAB`, `registry.tsx`

**화면 계약:**
- `CalendarScreen`: 다가오는 항목 목록(날짜·종류 태그·메모·반) + 추가 폼(날짜 `input type=date`, 종류 seg `휴원|특강|보강`, 메모, 반 선택 `전체|반…`) → `addCalendar` → toast "저장했어요. 그날은 다음 수업·결석 신청에서 빠져요" / 행 삭제.
- `Classes`: 반 행(이름 · 요일 시간 요약 · 담당 강사) → 펼치면 편집: 이름, 요일 7개 토글 + 시작·끝 시간(`input type=time`, 모든 요일 같은 시간 — 요일별 다른 시간은 5주차), 담당 강사 select(`listTeachers`) → `updateClass`. 맨 아래 `반 추가`.
- 휴원일 규칙: `calendar.kind='closed'` 이고 `class_id` 가 null 이거나 그 반이면 그 반의 수업일에서 뺀다. `closedDays()` 는 반 구분 없이 전체 휴원(`class_id is null`)만 Set 으로 돌려주고, 반별 휴원은 5주차(지금은 화면에 "반별 휴원은 준비 중" 문구 없이 `class_id` 선택만 저장).

- [ ] Step 1: 구현
- [ ] Step 2: 빌드 → 브라우저: 휴원일을 내일(2026-09-04 금)로 추가 → 학부모 박지훈 어머님(고1 A 월수금) 홈의 "다음 수업"이 9월 7일 월로 바뀜, 결석 신청 후보에서 9/4 빠짐 → 원장 오늘 화면에서 날짜를 9/4 로 고르면 휴원 안내 → 삭제. 보강 완결: 오늘 화면 처리됨의 한지우 행 `보강 왔어요` → 완료 태그, 한지우 상세 달력 9/5 에 ◌.
- [ ] Step 3: 커밋 — `feat(app): 휴원일·특강, 반 시간표·담당 강사, 오늘 화면 휴원 안내, 보강 완결`

---

### Task 6: FAQ 편집 · 자녀 둘 링크 · 문서·검증

**Files:**
- Modify: `app/src/screens/director/Inbox.tsx` `FaqManage` — 행 편집(질문·답 textarea) · 추가 · 삭제 (`saveFaq`/`deleteFaq`)
- Modify: `supabase/functions/link-login/index.ts` — `view in ('child')` 이고 `ref_id` 가 있으면 `absence_requests`/`attendance` 에서 `student_id` 를 찾아 그 자녀의 parent membership 을 활성으로
- Modify: `tools/outbox-test.mjs` — E 절에 자녀 둘 케이스는 5주차(메모만)
- Modify: `docs/superpowers/specs/2026-09-02-yeongeo-jip-stage2-architecture.md` §11 표 — 구현 상태 열 추가(✓ / 5주차)
- Modify: `docs/ops/meeting-guide.md` — 원장 미팅 때 "명부는 앱에서 직접 편집" 문구
- Create: `tests/app-verify-week4.png`

- [ ] Step 1: FAQ 편집 구현 → 브라우저에서 질문 하나 고치고 하나 추가·삭제
- [ ] Step 2: link-login 자녀 고르기 → `outbox-test` E 그대로 PASS(단일 자녀는 영향 없음) + 배포
- [ ] Step 3: 회귀 전체(`flow`·`rls`·`otp`·`outbox`·`manage`) · `npm test` · `npm run build` · 시트(명부·학생 편집·학생 상세 3단·휴원일·반 시간표·강사·FAQ) · 계획에 "검증 결과" · 커밋 → **사용자 확인 후 푸시**

---

## Self-Review

**Spec coverage (§11 "2단계에 넣는다")** — 학생이 반 여럿 ✓(2주차) · 명부 실제 편집·퇴원 → Task 1·3 ✓ · 출결 이력·통계 → Task 1(`month_attendance`)·4 ✓(월 달력·출석률; 반별 월 통계 표는 5주차 PC 표와 함께) · 강사 역할 → Task 2·3(명부·membership·담당 지정) △ **RLS 를 자기 반으로 좁히는 건 5주차**(계획 머리에 명시) · 상담·메모 → Task 4 ✓ · 시간표·휴원일 → Task 5 ✓(반별 휴원은 저장만, 적용은 5주차) · 보강 완결 → Task 1·5 ✓ · 학생별 타임라인 → Task 1·4 ✓. §5.6 PC 표·CSV 올리기·내보내기 → 5주차.

**Placeholder scan** — 화면 Task 는 2주차 방식(데이터 계약 + 요소 목록)이고 공유 코드(SQL·api 시그니처·테스트)는 전문. "5주차" 로 미룬 항목은 모두 이유가 적혀 있다.

**Type consistency** — RPC 이름: `roster_save_student(sid, p_name, p_class_ids, p_student_phone, p_parent_phones)` · `student_leave(sid)` · `student_timeline(sid, lim)` · `month_attendance(sid, ym)` · `makeup_attended(aid)` · `roster_of_student(sid)` · `roster_save_teacher(p_name, p_phone)` · `roster_remove_teacher(p_phone)` · `list_teachers()` — 테스트·api 표기 일치. api: `saveStudent`·`leaveStudent`·`monthAttendance`·`timeline`·`markMakeupAttended`·`closedDays`·`nextClassDays(schedule, count, closed?)`·`monthGrid(ym)`.

**확인 항목(실행 중)** — `memberships` unique 제약이 `(user_id, academy_id, role, student_id)` 인지(otp-verify 의 `onConflict` 와 같은 이름) · `attendance` 의 unique 가 `(student_id, class_id, date)` 인지(seed 가 그렇게 쓴다) · `absence_requests.attended_at` 존재(0001 ✓).

## 검증 결과 (2026-09-03)

- `manage-test` A~G PASS(새 학생 저장 → 학부모 otp 통과 → 반 이동·번호 교체·학생 사용자 연결/해제 → 기존 사용자 자동 연결·중복 저장 무해 → 메모·휴원일·보강 완결 → 월 출결(학부모)·타임라인(원장만)·강사 명부 → 퇴원 → otp 404·남은 세션 차단·원장은 퇴원생 열람). `flow`·`rls`·`otp`·`outbox` PASS, 단위 16/16, 빌드 OK.
- 브라우저(원장): 명부 → 학생 추가(고1 A, 학부모 010-1234-0999) → 그 번호 otp-send 200 → 편집으로 고2 B 이동 → 상세(빈 달력) → 퇴원 처리 → "퇴원 1명" 접힘 아래로, otp-send 404. 김민수 상세: 9월 달력에 오늘 지각(△), 메모 남기기, 기록 단에 상담·지각. 휴원일 9/4 추가. 반·시간표 고1 A 폼(월수금 19:00–21:00). 오늘 화면 한지우 "보강 왔어요" → 완료. FAQ 고치기·추가·지우기. 강사 넣기·빼기.
- 브라우저(학부모 박지훈 어머님): 휴원일 9/4 가 있으면 "다음 수업 9월 7일 월", 결석 신청 후보 9/7·9/9·9/11(9/4 없음). 확인 뒤 휴원일·테스트 학생·FAQ 수정은 되돌렸다.
- 시트 `tests/app-verify-week4.png`(명부 · 학생 편집 · 상세 출결 · 상세 기록 · 휴원일 · 반 시간표).
- 계획에서 바꾼 것
  - `memberships`·`roster_phones` unique 를 **NULLS NOT DISTINCT** 로 바꾸고 중복을 정리했다. 원장·강사 행은 `student_id` 가 null 이라 on conflict 가 안 잡혀 **로그인마다 원장 소속이 하나씩 늘고 있었다**(dev 프로젝트에서 9개). 문의·결석 알림이 소속 수만큼 겹쳐 오던 원인이기도 하다.
  - `student_timeline` 은 plpgsql 로 바꿔 staff 가 아니면 예외(빈 결과가 아니라). 반환 열 `at` → `ts`.
  - 학생 번호를 바꾸면 옛 사용자의 `students.user_id`·membership 을 풀고 새 번호 사용자를 무조건 잇는다.
  - `calendar` 의 unique 도 `class_id` null 문제가 있어 전체 휴원은 앱에서 찾고-고치기로 넣는다(다음 마이그레이션에서 같은 방식으로 고칠 것).
- 5주차로: 강사를 자기 반으로 좁히는 RLS, 반별 휴원 반영, 요일별 다른 시간, PC 넓은 화면 표·CSV·내보내기, 반별 월 통계, 학부모 화면의 월 달력, `calendar` unique 정리.
