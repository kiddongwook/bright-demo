# 이음새 검증 + 적대적 점검 계획 (2026-09-04)

> 이번 묶음(공지 템플릿·휴원일 연동·날짜/시간 선택, 수강료 수기 관리, 보강 칩·출석 사유·반 자동 선택·답변 템플릿, 할 것 칩·휴원일 기간/반복, 푸시·초대 링크)이 끝난 뒤, 화면 단위가 아니라 **데이터가 서로 맞물리는 자리**를 검증한다. 기준: 상호연동 · 중복 입력 제어 · 입출력과 뷰잉 일치.

## 1. 이음새 시나리오 (스크립트 `tools/seam-test.mjs` + 헤드리스 UI)

| # | 이음새 | 시나리오 | 기대 |
|---|---|---|---|
| S1 | 공지 ↔ 휴원일 | 휴원 안내 공지(날짜 D, 보강일 M, 반 C) 올리기 | notices 1행, calendar closed(D,C) 1행 + makeup(M,C) 1행 |
| S2 | 〃 중복 | 같은 날짜로 한 번 더 올리기 | 공지는 2행(공지는 중복 허용), calendar 는 여전히 1행씩, 사용자에게 "이미 있어요" 안내 |
| S3 | 휴원일 → 학부모 | D 가 학부모 다음 수업·결석 신청 후보에서 빠짐 | `nextClassDaysFor` 결과에 D 없음 |
| S4 | 휴원일 기간·반복 | 기간 3일 넣고 다시 같은 기간 넣기 / 매주 4주 | 첫 3행, 두 번째 0행(건너뜀 3), 매주 4행 |
| S5 | 출석 사유 ↔ 알림 | 지각 + 사유 "10분" 저장 | attendance.note='10분', 학부모 notifications 제목/본문에 사유, outbox push 행 1(구독 시) |
| S6 | 〃 중복 | 같은 날 같은 학생을 두 번 저장(상태 동일) | attendance 1행(upsert), notifications 1행(멱등) |
| S7 | 출석 상태 변경 | 출석 → 결석으로 바꿔 저장 | attendance 1행 갱신, 새 알림 1행(상태 바뀜) |
| S8 | 수강료 ↔ 학생 | 학생 2명(형제) + 반 요금제 → `issue_invoices` 두 번 | 첫 2행(형제 할인 1명), 두 번째 0행 |
| S9 | 〃 퇴원·재입학 | 퇴원생은 청구 제외, 재입학 뒤 다시 청구 가능 | 퇴원 후 issue 0, 재입학 후 1 |
| S10 | 〃 납부 | 부분 → 전액 | partial → paid, payments 합계 = total, 학부모 `my_invoice` 상태 일치 |
| S11 | 〃 안내 | `remind_unpaid` 두 번(20시간 안) | 첫 N, 두 번째 0; notifications 학부모 행 N |
| S12 | 초대 ↔ 명부 | 초대 토큰 발급 뒤 명부에서 번호 삭제 → 링크 사용 | 401(명부에 없음) |
| S13 | 〃 재발급 | 같은 번호로 새 토큰 | 옛 토큰 401 used/expired, 새 토큰 200 |
| S14 | 푸시 ↔ 카톡 | 구독 없음 → 공지 / 구독 → 공지 / kakao_also → 공지 / 구독 삭제 → 공지 | sms / push / push+sms / sms |
| S15 | 보강 칩 ↔ 결석 | 결석 신청에 보강일 칩 선택 → 확정 | absence.makeup_at = 칩 시각, 학부모 알림 MAKEUP_CONFIRMED 1행 |
| S16 | 답변 → FAQ | "FAQ 에도 올리기" 체크 → 답변 | inquiries answered, faq 1행, 두 번 눌러도 faq 1행 |
| S17 | 반 자동 선택 | 지금 수업 중인 반이 있을 때 오늘 탭 | 그 반이 선택됨(순수 함수 단위 테스트 + UI) |
| S18 | 뷰잉 일치 | 원장 수강료 화면 합계 = invoices total 합 = 학부모 카드 금액 | 숫자 동일 |

## 2. 적대적 점검 (에이전트 3종, 읽기 전용 + 자기 데이터만 쓰기)

- **보안**: 다른 학원 원장 JWT 로 `issue_invoices`/`record_payment`/`create_invite`/`save_attendance_v2`; 학부모 JWT 로 원장 RPC; 초대 토큰 추측(짧은/잘못된 형식); push_subscriptions 에 남의 user_id 로 insert; 고정 인증번호를 다른 번호에 시도; `my_invoice` 로 남의 자녀 조회.
- **데이터 무결성**: 같은 요청 동시 2회(issue_invoices, record_payment, addCalendar 기간), 퇴원 연쇄(invoices·payments·push_subscriptions 잔존 여부), 학생 삭제 뒤 고아 행, 초대 토큰 만료 정리, outbox 멱등키 충돌.
- **입력**: 제목 2,000자·이모지·줄바꿈이 알림톡 파라미터/푸시 본문에 들어갈 때, 날짜 `2026-02-30`·과거 날짜·시간 `25:00`, 금액 음수·소수·10억, 전화번호 `+82 10-…`, 빈 문자열.

각 에이전트는 **발견 → 재현 스크립트 → 심각도(높음/중간/낮음)** 표로 보고. Fable 이 고칠 것을 정하고 수정 → 재검증 → 배포.

## 3. 화면 검증 (같은 무게로)

- 전수 스크린샷: 원장·학부모·학생 × 밝음·어두움(`scratchpad/sweep/sweep.mjs`) + PC·데스크톱 프레임. 이번 묶음의 새 화면을 목록에 추가: 공지 쓰기(템플릿 필드 채운 상태·미리보기 펼침), 휴원일 추가(기간·매주 모드), 수강료 목록·학생 시트·수강료 설정, 학부모 수강료 카드, 출석 사유 시트, 보강 칩, 답변 템플릿 칩, 할 것 칩, 시간 선택 시트, 날짜 선택 칩.
- 보는 기준: 토큰 밖 색 없음, 카드 안 요소가 카드 반지름 규칙(20/14/8) 을 따름, 입력칸 48px·터치 44px, 긴 문구 줄바꿈·말줄임, 빈 상태·오류 상태·로딩 스켈레톤이 새 화면에도 있음, 다크에서 대비.
- 시트는 `tests/app-verify-batch2-*.png` 로 저장하고 문서에 결과를 적는다.

## 4. 결과 (2026-09-04)

- 이음새 S1~S18: 전부 PASS(`tools/seam-test.mjs`). 처음 실패했던 S5(푸시 본문에 출결 사유 없음 → 0016 + push.ts) 와 S16(답변→FAQ 중복 → 0017 unique + `saveFaqDedup`) 수정.
- 적대적 점검: 보안 3건(중간 1 · 낮음 2) → 0017 로 모두 수정, 재검사 "all held". 무결성 24건(중간 13 · 낮음 11) → 중간 13건 0018/0019 로 수정(INT-06·11·26·35·36 낮음 5건과 INT-20 일부·INT-22 반 공지 restrict 는 남김). 입력 높음 5 · 중간 15 → Edge(발송기 예외 방어·죽은 구독 문자 대체·본문 자르기·전화번호 형식)와 화면(글자 수 상한·CSV 동명이인 보호·시간 검증)·DB(형식 제약) 로 수정. 보고서: `docs/reports/2026-09-04-redteam-{security,integrity,input}.md`, 재현 스크립트 `tools/redteam/`.
- 배포 결함: 웹 푸시 발송이 "개인키 해독 실패" → VAPID 개인키를 PKCS8 로도 넣고(`VAPID_PRIVATE_PKCS8`) `push.ts` 가 그것을 우선 사용. 프로브 `tools/push-probe.mjs` 로 서명까지 통과 확인.
- 새 스위트: `hardening-test.mjs`(10절), `seam-test.mjs`(18절). 전체 회귀 10종 PASS, 단위 124/124.
- 화면: 75장 재촬영. 새 홈 제목("9월 4일 금요일")·홈 탭, 결석 신청 선택기, 문의의 "기록 보기"·"메모로도 남기기", 글자 수 카운터 확인. 시트 `tests/app-verify-batch2-{1..5}.png`.
- 남은 것: 실제 기기 푸시 수신(사용자 확인 필요), 낮음 항목들(보고서 참조), 반 공지가 걸린 반 삭제 안내 문구.
